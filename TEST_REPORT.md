# 測試報告

日期：2026-09-14

## 結論與限制

網站已完成實作，通過程式語法、資料與資源檢查，以及 jsdom 26.1.0 的 DOM 整合測試。測試瀏覽器兩次嘗試開啟預覽都回傳 `ERR_BLOCKED_BY_CLIENT`；這是本次預覽環境的存取限制，無法據此判定網站在使用者裝置上的表現。

**沒有把 DOM 測試稱為真實瀏覽器測試。動畫視覺、真實鍵盤行為、版面無溢出和各裝置相容性尚未實測，不宣稱全部驗收完成。** 本專案尚未發布到使用者 GitHub。

## 已完成的檢查

| 檢查 | 結果與方法 |
| --- | --- |
| JavaScript 語法 | `script.js`、`personality-data.js` 皆通過語法檢查 |
| 資料完整 | 16 種不重複的代號、四組映射、字母拆解與來源全部齊全 |
| 正文篇幅 | 各型約 830–880 個中文字；計入十個主要內容欄位與反思題，不含頁面共用提醒 |
| 資訊架構 | 每型 2 段概述、4 個校園例子、5 項學習內容、3 項合作內容、4 項優勢、4 項盲點、2 段壓力、3 項成長、2 則情境與3題反思 |
| 全部人格開啟與返回 | DOM 實際派發點擊事件，16 型全部驗證；每頁都有 10 個內容區塊 |
| 快速導覽 | 未選完時不可開啟；選擇 ENFP 後正確開啟活動家 |
| 跨人格瀏覽 | 下拉切換、底部 4 個差一字母及 2 個差異較多的入口皆有正確資料 |
| 歷史導覽 | DOM 模擬環境中的 Back、Forward、Escape 與總覽捲動位置恢復通過 |
| 快速連續操作 | 開啟中收到 Escape，能最終回到乾淨的總覽狀態 |
| URL hash | 直接開啟 `#intj`、`#enfp`，以及不存在的 hash 復原通過 |
| 子路徑 | 以 `/course/personality-explorer/` 模擬網站位置；相對資源與 hash 正常 |
| 舊瀏覽器路徑 | 未提供 View Transitions API 的 DOM 環境仍能開啟與返回；移動採 CSS 轉場 |
| 減少動態 | 有獨立 CSS 規則停用移動／縮放；模擬 reduced-motion 條件下，功能測試通過 |
| 焦點恢復 | DOM 測試確認開啟後焦點至人格標題、返回後至原入口；跳至主要內容不修改人格 hash |
| JavaScript 關閉 | HTML 本身仍有標題、基本說明、16 個稱號與短標語、21 個來源 |
| DOM 重複 ID | 初始 HTML 沒有重複 ID |
| 本地資源 | CSS / JS 均為 `./` 相對路徑，檔案存在；沒有硬編碼本機路徑或本機服務網址 |
| DOM 執行錯誤 | 整合測試中沒有捕捉到 JavaScript 錯誤；真實瀏覽器 Console 仍待驗 |

## 指定文字色彩對比

以下依 sRGB 相對亮度公式計算，並非對所有渲染狀態的完整無障礙稽核。分類主色維持 YAML 指定色；正文或小字使用較深的文字色，尤其黃色文字額外加深，以維持可讀性。

| 用途 | 前景／背景 | 對比 |
| --- | --- | --- |
| 主要文字 | `#2F3A46` / `#F7F9FA` | 10.96:1 |
| 次要文字 | `#596774` / `#FFFFFF` | 5.81:1 |
| 紫組文字 | `#684877` / `#F2ECF5` | 6.50:1 |
| 綠組文字 | `#216B4C` / `#EAF6F0` | 5.79:1 |
| 藍組文字 | `#2A677B` / `#EAF4F7` | 5.65:1 |
| 黃組文字 | `#805B13` / `#FBF3DF` | 5.55:1 |
| 主按鈕 | `#FFFFFF` / `#2F6474` | 6.57:1 |

主文為 16px；部分輔助標籤較小。卡片為原生 button，字母選擇為原生 radio；最小字母按鈕使用 44px 寬與44px高，窄螢幕改為兩組一排。

## 尚待真實瀏覽器驗證

| 尺寸／裝置 | 預期版面 | 本次狀態 |
| --- | --- | --- |
| 320 × 740，窄手機 | 卡片單欄、字母選擇兩組一排 | 已寫響應式規則，未渲染實測 |
| 390 × 844，iPhone 常見尺寸 | 卡片單欄、詳細頁單欄 | 未實測 |
| 412 × 915，Android 手機常見尺寸 | 卡片單欄、詳細頁單欄 | 未實測 |
| 768 × 1024，平板直式 | 每組 2 × 2 卡片 | 未實測 |
| 820 × 1180，平板直式 | 每組 2 × 2 卡片 | 未實測 |
| 1024 × 768，平板橫式 | 每組四張卡片、正文雙欄 | 未實測 |
| 1440 × 900，桌機 | 每組四張卡片、最大寬度 1180px | 未實測 |
| Chrome / Edge / Safari | 原生功能與 CSS 轉場 | 未實測 |

## 課堂使用前的驗收清單

1. 上傳 GitHub Pages，打開實際公開網址；頁面應顯示四組與16張卡片。
2. 在手機與平板各點選一個位於畫面下方的人格：選取卡片放大、其餘淡出、標題移到上方中央、內容淡入；不得卡住或橫向溢出。
3. 看完長頁面後返回；捲動位置與焦點應合理恢復。也測試開啟過程按 Escape。
4. 依序測試各型、下拉切換、差異入口、瀏覽器上一頁／下一頁，以及直接開啟帶 hash 的網址。
5. 用 Tab 選擇卡片，分別用 Enter 與 Space 開啟；焦點框應清楚。radio 可用方向鍵切換。
6. 啟用系統「減少動態效果」，確認沒有大幅移動或縮放，功能仍完整。
7. 將瀏覽器放大至200%，檢查文字與控制項沒有重疊或被裁切。
8. 開啟瀏覽器 Console，確認沒有程式或資源錯誤；網路清單不應有必要資源404。
9. 將 JavaScript 停用後重開首頁，確認基本文字與16型總覽仍可閱讀。
10. 最後以學生手機掃描首頁 QR Code，確認是網站頁面而非程式碼頁。

這些待驗項目沒有勾選為通過，也沒有使用模擬截圖充當實際驗收證據。
